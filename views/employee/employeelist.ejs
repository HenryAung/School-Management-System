<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Home Page</title>
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css">
</head>

    <!-- Include the top menu -->
    <%- include('../componants/navbar') %>


<body class="font-sans bg-gray-100">

    <div class="flex">
        
        <!-- Include the side menu -->
        <%- include('../componants/sidemenu') %>

        <!-- Main Content -->
        <main class="flex-1 p-4">
            <div class="container mx-auto my-10">
                <h1 class="text-3xl font-bold mb-6">Employee Lists</h1>
                <table class="min-w-full border border-gray-300">
                    <thead>
                        <tr>
                            <th class="border border-gray-300 py-2 px-4 bg-gray-100">No.</th>
                            <th class="border border-gray-300 py-2 px-4 bg-gray-100">Name</th>
                            <th class="border border-gray-300 py-2 px-4 bg-gray-100">Age</th>
                            <th class="border border-gray-300 py-2 px-4 bg-gray-100">Class</th>
                            <th class="border border-gray-300 py-2 px-4 bg-gray-100">Action</th>
                        </tr>
                    </thead>
                    <tbody>
                        
                    </tbody>
                </table>
            </div>

          
        </main>
    </div>
    

  

</body>
</html>
